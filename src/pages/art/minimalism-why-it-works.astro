---
import Layout from "../../layouts/Layout.astro";

const title = "Minimalism: Why Less Often Feels Better — Art";
const updated = "Updated: 2026";
---

<Layout title={title}>
  <article class="page">
    <!-- HERO -->
    <header class="hero">
      <div class="hero-top">
        <a class="back" href="/art">← Back to Art</a>
        <span class="chip chip--design">DESIGN</span>
      </div>

      <div class="hero-grid">
        <div class="hero-left">
          <h1>Minimalism: Why Less Often Feels Better</h1>
          <p class="lead">Minimalism works when it reduces noise so the signal becomes obvious. It’s not ‘less’ for its own sake — it’s clarity as a value.</p>

          <div class="meta">
            <span class="meta-pill">Clarity • Reduction</span>
            <span class="meta-pill">{updated}</span>
          </div>
        </div>

        <div class="hero-right card">
          <img class="hero-img" src="https://images.unsplash.com/photo-1526481280695-3c687fd643ed?w=200&q=80" alt="Minimalism: Why Less Often Feels Better" loading="lazy" />
          <div class="hero-img-cap">Less noise, more meaning.</div>
        </div>
      </div>
    </header>

    <!-- TLDR -->
    <section class="card section">
      <h2>TL;DR</h2>
      <div class="tldr">
        <div class="tldr-item">
          <div class="dot" aria-hidden="true"></div>
          <div>
            <h3>Cognitive ease</h3>
            <p>Fewer elements = fewer decisions, so the viewer relaxes.</p>
          </div>
        </div>
        <div class="tldr-item">
          <div class="dot" aria-hidden="true"></div>
          <div>
            <h3>Contrast becomes loud</h3>
            <p>When everything is quiet, one bold choice stands out.</p>
          </div>
        </div>
        <div class="tldr-item">
          <div class="dot" aria-hidden="true"></div>
          <div>
            <h3>Restraint suggests confidence</h3>
            <p>Minimal design often reads as premium because it feels intentional.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- BODY -->
    <section class="section">
      <div class="two-col">
        <div class="card pad">
          <h2>Minimalism is subtraction with purpose</h2>
          <p>Remove anything that doesn’t support the message. If you can’t explain why an element exists, it’s probably decoration.</p>
          <p>The goal is not emptiness — it’s focus.</p>

          <div class="callouts">
            <div class="callout">
              <h4>A practical rule</h4>
              <p>One layout, one primary action. Make everything else supportive and secondary.</p>
            </div>
            <div class="callout">
              <h4>Whitespace isn’t ‘empty’</h4>
              <p>Whitespace is spacing that helps the eye separate groups and understand hierarchy.</p>
            </div>
          </div>
        </div>

        <div class="card pad">
          <h2>Where minimalism fails</h2>
          <p>Minimalism becomes sterile when it removes personality and context. Clarity needs a voice.</p>

          <div class="grid3">
            <div class="mini">
              <b>Too little hierarchy</b>
              <p>If everything is the same weight, nothing feels important.</p>
            </div>
            <div class="mini">
              <b>No texture</b>
              <p>Subtle texture, type, or motion can keep minimal designs from feeling dead.</p>
            </div>
            <div class="mini">
              <b>Mismatch with content</b>
              <p>Complex topics may require richer structure, not fewer words.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- FAQ -->
    <section class="card section">
      <h2>FAQ</h2>

      <div class="faq">
        <details>
          <summary>Is minimalism always better?</summary>
          <p>No. It’s a tool — maximalism can communicate abundance, warmth, or chaos intentionally.</p>
        </details>
        <details>
          <summary>How do I keep it human?</summary>
          <p>Use tone, microcopy, and small imperfections (texture, analog photos, gentle motion).</p>
        </details>
        <details>
          <summary>Does minimalism help conversion?</summary>
          <p>Often yes, because it reduces distraction — but only if the offer is clear.</p>
        </details>
      </div>

      <div class="footnav">
        <a class="btn btn--ghost" href="/art/film-editing-invisible-art">← Editing: The Invisible Art That Creates Meaning</a>
        <a class="btn" href="/art/photography-light">Light: The One Thing Every Great Photo Shares →</a>
      </div>
    </section>
  </article>

  <style>
    .page{ max-width: 980px; margin: 0 auto; padding: 24px 16px 60px; }
    .hero{ display:flex; flex-direction: column; gap: 16px; margin-top: 10px; }
    .hero-top{ display:flex; align-items:center; justify-content: space-between; gap: 12px; }
    .back{ color: rgba(11,11,15,0.62); text-decoration:none; font-size: 14px; }
    .back:hover{ text-decoration: underline; }

    .chip{
      font-size: 12px;
      letter-spacing: .14em;
      text-transform: uppercase;
      padding: 8px 10px;
      border-radius: 999px;
      background: rgba(245,245,247,0.9);
      border: 1px solid rgba(11,11,15,0.08);
    }
    .chip--music{ box-shadow: 0 0 0 2px rgba(0,0,0,0.02), inset 0 0 0 1px rgba(120,90,255,0.22); }
    .chip--painting{ box-shadow: 0 0 0 2px rgba(0,0,0,0.02), inset 0 0 0 1px rgba(255,120,90,0.22); }
    .chip--architecture{ box-shadow: 0 0 0 2px rgba(0,0,0,0.02), inset 0 0 0 1px rgba(90,180,255,0.22); }
    .chip--film{ box-shadow: 0 0 0 2px rgba(0,0,0,0.02), inset 0 0 0 1px rgba(255,200,90,0.22); }
    .chip--design{ box-shadow: 0 0 0 2px rgba(0,0,0,0.02), inset 0 0 0 1px rgba(90,255,190,0.22); }
    .chip--photography{ box-shadow: 0 0 0 2px rgba(0,0,0,0.02), inset 0 0 0 1px rgba(160,160,160,0.30); }

    .hero-grid{ display:grid; grid-template-columns: 1.2fr .8fr; gap: 14px; align-items: stretch; }
    h1{ font-size: 44px; line-height: 1.05; margin: 0; }
    .lead{ font-size: 16px; line-height: 1.7; color: rgba(11,11,15,0.72); margin: 10px 0 0; }
    .meta{ margin-top: 14px; display:flex; flex-wrap: wrap; gap: 8px; }
    .meta-pill{
      font-size: 12px;
      padding: 7px 10px;
      border-radius: 999px;
      background: rgba(245,245,247,0.8);
      border: 1px solid rgba(11,11,15,0.08);
      color: rgba(11,11,15,0.70);
    }

    .card{
      background: rgba(255,255,255,0.72);
      border: 1px solid rgba(11,11,15,0.08);
      border-radius: 18px;
      box-shadow: 0 8px 30px rgba(0,0,0,0.06);
      overflow: hidden;
    }
    .hero-img{ width: 100%; height: 100%; object-fit: cover; display:block; min-height: 220px; }
    .hero-img-cap{
      padding: 10px 12px;
      font-size: 12px;
      color: rgba(11,11,15,0.62);
      border-top: 1px solid rgba(11,11,15,0.08);
      background: rgba(245,245,247,0.65);
    }

    .section{ margin-top: 14px; }
    .section h2{ font-size: 20px; margin: 0 0 10px; }
    .pad{ padding: 16px; }
    .section.card{ padding: 16px; }

    .tldr{ display:grid; grid-template-columns: 1fr; gap: 10px; }
    .tldr-item{ display:flex; gap: 10px; align-items: flex-start; padding: 10px 10px; border-radius: 14px; background: rgba(245,245,247,0.6); border: 1px solid rgba(11,11,15,0.06); }
    .dot{ width: 10px; height: 10px; border-radius: 999px; margin-top: 6px; background: rgba(11,11,15,0.28); }
    .tldr-item h3{ font-size: 14px; margin: 0; }
    .tldr-item p{ margin: 6px 0 0; font-size: 14px; line-height: 1.7; color: rgba(11,11,15,0.68); }

    .two-col{ display:grid; grid-template-columns: 1fr 1fr; gap: 14px; }
    .two-col p{ font-size: 14px; line-height: 1.75; color: rgba(11,11,15,0.72); margin: 10px 0 0; }
    .callouts{ display:grid; grid-template-columns: 1fr; gap: 10px; margin-top: 14px; }
    .callout{ padding: 12px; border-radius: 14px; background: rgba(245,245,247,0.7); border: 1px solid rgba(11,11,15,0.06); }
    .callout h4{ margin: 0; font-size: 13px; }
    .callout p{ margin: 6px 0 0; font-size: 13px; line-height: 1.7; color: rgba(11,11,15,0.66); }

    .grid3{ display:grid; grid-template-columns: 1fr; gap: 10px; margin-top: 12px; }
    .mini{ padding: 12px; border-radius: 14px; background: rgba(245,245,247,0.65); border: 1px solid rgba(11,11,15,0.06); }
    .mini b{ font-size: 13px; }
    .mini p{ margin: 6px 0 0; font-size: 13px; line-height: 1.7; color: rgba(11,11,15,0.66); }

    .faq{ display:grid; gap: 10px; }
    details{ border: 1px solid rgba(11,11,15,0.08); border-radius: 14px; padding: 10px 12px; background: rgba(245,245,247,0.55); }
    details summary{ cursor:pointer; list-style:none; font-weight: 600; }
    details summary::-webkit-details-marker{ display:none; }
    details p{ margin: 10px 0 0; font-size: 14px; line-height: 1.7; color: rgba(11,11,15,0.62); }

    .footnav{ margin-top: 16px; display:flex; justify-content: space-between; gap: 12px; }
    .btn{
      display:inline-flex;
      align-items:center;
      justify-content:center;
      gap: 8px;
      padding: 10px 12px;
      border-radius: 999px;
      border: 1px solid rgba(11,11,15,0.10);
      background: rgba(11,11,15,0.92);
      color: white;
      text-decoration:none;
      font-size: 14px;
      white-space: nowrap;
    }
    .btn:hover{ opacity: 0.92; }
    .btn--ghost{ background: rgba(245,245,247,0.75); color: rgba(11,11,15,0.86); }

    @media (max-width: 900px){
      .hero-grid{ grid-template-columns: 1fr; }
      .two-col{ grid-template-columns: 1fr; }
    }
    @media (max-width: 480px){
      h1{ font-size: 34px; }
      .footnav{ flex-direction: column; }
      .btn{ width: 100%; }
    }
  </style>
</Layout>
