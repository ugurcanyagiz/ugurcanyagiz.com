---
import Layout from "../../layouts/Layout.astro";

const title = "What Is Time, Really? — Science";
const updated = "Updated: 2026";
---

<Layout title={title}>
  <article class="page">
    <!-- HERO -->
    <header class="hero">
      <div class="hero-top">
        <a class="back" href="/science">← Back to Science</a>
        <span class="chip chip--physics">PHYSICS</span>
      </div>

      <h1>What Is Time, Really?</h1>
      <p class="subtitle">
        Time feels like a flowing river, but physics treats it like a <b>dimension</b>. And it doesn’t “flow”
        the same way for everyone — motion and gravity change how fast clocks tick.
      </p>

      <div class="hero-grid">
        <section class="card card--soft">
          <h2 class="hsmall">What you’ll learn</h2>
          <ul class="list">
            <li>Why time is measured by <b>change</b> and <b>clocks</b>.</li>
            <li><b>Time dilation</b>: fast motion and gravity slow clocks.</li>
            <li>Why “now” is not universal (relativity of simultaneity).</li>
          </ul>
        </section>

        <section class="card card--ink">
          <p class="quote">
            “Time isn’t one global metronome. Different paths through spacetime can accumulate different amounts of time.”
          </p>
          <p class="meta">{updated}</p>
        </section>
      </div>
    </header>

    <!-- TL;DR -->
    <section class="section">
      <h2>TL;DR</h2>
      <div class="tldr">
        <div class="tldr-item">
          <div class="dot dot--a" aria-hidden="true"></div>
          <div>
            <h3>Time is what clocks measure</h3>
            <p>We define time operationally: by repeating physical processes (ticks).</p>
          </div>
        </div>
        <div class="tldr-item">
          <div class="dot dot--b" aria-hidden="true"></div>
          <div>
            <h3>Motion slows your clock</h3>
            <p>At high speeds, your time passes more slowly compared to someone at rest.</p>
          </div>
        </div>
        <div class="tldr-item">
          <div class="dot dot--c" aria-hidden="true"></div>
          <div>
            <h3>Gravity slows your clock</h3>
            <p>Closer to massive objects, time runs slower compared to farther away.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- SECTION: What time is -->
    <section class="section">
      <h2>So… what is time?</h2>
      <p class="lead">
        In everyday life, time feels like a smooth flow. In physics, time is a coordinate we use to describe
        change — and we anchor it to <b>clocks</b>. A “clock” can be anything that ticks reliably: atoms vibrating,
        light oscillating, or a crystal resonating.
      </p>

      <div class="two-col">
        <div class="card">
          <h3 class="hsmall">Two useful viewpoints</h3>
          <div class="stack">
            <div class="callout">
              <h4>Operational time</h4>
              <p>Time is defined by what a clock reads.</p>
            </div>
            <div class="callout">
              <h4>Spacetime time</h4>
              <p>
                In relativity, time is part of a 4D geometry (spacetime). Different observers slice “now” differently.
              </p>
            </div>
            <div class="callout">
              <h4>Psychological time</h4>
              <p>
                Our brain constructs a feeling of flow. That feeling is real to us — but it’s not a law of physics.
              </p>
            </div>
          </div>
        </div>

        <figure class="figure card">
          <figcaption class="figcap"><b>Time as “ticks”</b> — counting regular change</figcaption>

          <svg viewBox="0 0 640 360" role="img" aria-label="Clock ticks diagram showing repeated cycles and counting ticks">
            <rect x="0" y="0" width="640" height="360" fill="rgba(245,245,247,1)"/>

            <!-- tick marks -->
            <g stroke="rgba(0,0,0,0.12)" stroke-width="2">
              <line x1="60" y1="210" x2="580" y2="210"/>
              <line x1="60" y1="210" x2="60" y2="220"/>
              <line x1="160" y1="210" x2="160" y2="220"/>
              <line x1="260" y1="210" x2="260" y2="220"/>
              <line x1="360" y1="210" x2="360" y2="220"/>
              <line x1="460" y1="210" x2="460" y2="220"/>
              <line x1="560" y1="210" x2="560" y2="220"/>
            </g>

            <!-- wave -->
            <path d="M60 160
                     C110 110, 160 210, 210 160
                     C260 110, 310 210, 360 160
                     C410 110, 460 210, 510 160
                     C560 110, 610 210, 660 160"
                  fill="none" stroke="rgba(0,122,255,0.95)" stroke-width="4" stroke-linecap="round"
                  transform="translate(-20,0)"/>

            <!-- labels -->
            <text x="320" y="70" text-anchor="middle" font-size="14" fill="rgba(11,11,15,0.75)">
              A clock is a repeatable physical cycle
            </text>
            <text x="320" y="255" text-anchor="middle" font-size="13" fill="rgba(11,11,15,0.60)">
              Counting cycles (“ticks”) is how we measure time.
            </text>

            <!-- tick numbers -->
            <g fill="rgba(11,11,15,0.55)" font-size="12">
              <text x="58" y="245">0</text>
              <text x="155" y="245">1</text>
              <text x="255" y="245">2</text>
              <text x="355" y="245">3</text>
              <text x="455" y="245">4</text>
              <text x="555" y="245">5</text>
            </g>
          </svg>

          <p class="note">
            Atomic clocks use extremely stable oscillations of atoms — that stability makes modern GPS possible.
          </p>
        </figure>
      </div>
    </section>

    <!-- SECTION: Special relativity -->
    <section class="section">
      <h2>Time dilation from motion (Special Relativity)</h2>
      <p class="lead">
        If you move very fast relative to someone else, your clock ticks more slowly <i>from their perspective</i>.
        This isn’t a trick — it’s measured in particle experiments and is built into GPS corrections.
      </p>

      <figure class="figure card">
        <figcaption class="figcap"><b>Moving clocks tick slower</b> — simple comparison</figcaption>

        <svg viewBox="0 0 860 320" role="img" aria-label="Two timelines comparing stationary clock ticks vs moving clock ticks">
          <rect x="0" y="0" width="860" height="320" fill="rgba(245,245,247,1)"/>

          <!-- stationary -->
          <text x="170" y="56" text-anchor="middle" font-size="13" fill="rgba(11,11,15,0.65)">Observer A (rest)</text>
          <line x1="170" y1="90" x2="170" y2="260" stroke="rgba(0,122,255,0.85)" stroke-width="5" stroke-linecap="round"/>
          <g fill="rgba(0,122,255,0.85)">
            <circle cx="170" cy="120" r="6"/>
            <circle cx="170" cy="150" r="6"/>
            <circle cx="170" cy="180" r="6"/>
            <circle cx="170" cy="210" r="6"/>
            <circle cx="170" cy="240" r="6"/>
          </g>

          <!-- moving -->
          <text x="650" y="56" text-anchor="middle" font-size="13" fill="rgba(11,11,15,0.65)">Observer B (moving fast)</text>
          <line x1="650" y1="90" x2="650" y2="260" stroke="rgba(255,45,85,0.85)" stroke-width="5" stroke-linecap="round"/>
          <g fill="rgba(255,45,85,0.85)">
            <circle cx="650" cy="130" r="6"/>
            <circle cx="650" cy="170" r="6"/>
            <circle cx="650" cy="210" r="6"/>
            <circle cx="650" cy="250" r="6"/>
          </g>

          <!-- annotation -->
          <line x1="220" y1="120" x2="600" y2="130" stroke="rgba(0,0,0,0.20)" stroke-width="2"/>
          <line x1="220" y1="240" x2="600" y2="250" stroke="rgba(0,0,0,0.20)" stroke-width="2"/>

          <text x="430" y="295" text-anchor="middle" font-size="13" fill="rgba(11,11,15,0.60)">
            Same “interval” in one frame can correspond to fewer ticks in the moving clock (time dilation).
          </text>
        </svg>

        <p class="note">
          At everyday speeds the effect is tiny. Near light speed it becomes dramatic.
        </p>
      </figure>
    </section>

    <!-- SECTION: Gravity -->
    <section class="section">
      <h2>Time dilation from gravity (General Relativity)</h2>
      <p class="lead">
        Gravity isn’t just a force — it’s curvature of spacetime. Clocks deeper in a gravitational field tick more slowly.
        That’s why GPS satellites need relativity corrections to stay accurate.
      </p>

      <figure class="figure card">
        <figcaption class="figcap"><b>Gravity slows time</b> — clocks at different heights</figcaption>

        <svg viewBox="0 0 860 320" role="img" aria-label="Gravity time dilation diagram with two clocks at different heights above Earth">
          <rect x="0" y="0" width="860" height="320" fill="rgba(245,245,247,1)"/>

          <!-- earth -->
          <circle cx="430" cy="360" r="200" fill="rgba(0,122,255,0.16)"/>
          <circle cx="430" cy="360" r="200" fill="none" stroke="rgba(0,0,0,0.10)" stroke-width="2"/>

          <!-- clocks -->
          <g>
            <rect x="210" y="70" width="120" height="120" rx="18" fill="rgba(255,255,255,0.92)" stroke="rgba(0,0,0,0.08)"/>
            <text x="270" y="205" text-anchor="middle" font-size="12" fill="rgba(11,11,15,0.55)">higher</text>
            <text x="270" y="46" text-anchor="middle" font-size="13" fill="rgba(11,11,15,0.65)">Clock A</text>

            <rect x="530" y="140" width="120" height="120" rx="18" fill="rgba(255,255,255,0.92)" stroke="rgba(0,0,0,0.08)"/>
            <text x="590" y="275" text-anchor="middle" font-size="12" fill="rgba(11,11,15,0.55)">lower</text>
            <text x="590" y="116" text-anchor="middle" font-size="13" fill="rgba(11,11,15,0.65)">Clock B</text>
          </g>

          <!-- arrows -->
          <g stroke="rgba(255,45,85,0.85)" stroke-width="4" stroke-linecap="round">
            <line x1="430" y1="215" x2="430" y2="260"/>
            <polygon points="430,260 420,246 440,246" fill="rgba(255,45,85,0.85)"/>
          </g>

          <text x="430" y="305" text-anchor="middle" font-size="13" fill="rgba(11,11,15,0.60)">
            Deeper in gravity → slower ticking compared to a higher clock.
          </text>
        </svg>

        <p class="note">
          This effect is real and measured. Without it, GPS errors would grow quickly.
        </p>
      </figure>
    </section>

    <!-- SECTION: The “now” problem -->
    <section class="section">
      <h2>Is there a universal “now”?</h2>
      <p class="lead">
        In relativity, simultaneity depends on the observer. Two events that are “at the same time” for you
        might not be “at the same time” for someone moving relative to you.
      </p>

      <div class="card">
        <div class="grid3">
          <div class="mini">
            <h4>Local now</h4>
            <p>In your immediate area, you can define a practical “now.”</p>
          </div>
          <div class="mini">
            <h4>No global now</h4>
            <p>Across large distances and relative motion, “now” becomes frame-dependent.</p>
          </div>
          <div class="mini">
            <h4>What remains solid</h4>
            <p>Physics focuses on what is measurable and invariant, not on one preferred present.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Explain to a child -->
    <section class="section">
      <h2>Explain it to a child</h2>
      <div class="card">
        <p class="lead">
          Time is like the “number of ticks” your personal clock counts.
        </p>
        <ol class="steps">
          <li>If you go very fast, your clock ticks a tiny bit slower than mine.</li>
          <li>If you are closer to something very heavy (like a planet), your clock also ticks a tiny bit slower.</li>
          <li>So different people can honestly measure different amounts of time — both can be correct.</li>
        </ol>
        <p class="note">
          It’s weird, but it’s true — and our technology (like GPS) depends on it.
        </p>
      </div>
    </section>

    <!-- FAQ -->
    <section class="section">
      <h2>FAQ</h2>
      <div class="faq">
        <details class="card" open>
          <summary>Does time “flow”?</summary>
          <p>
            Physics doesn’t require a flowing substance. We experience flow psychologically, while physics
            describes relationships between events and measurements.
          </p>
        </details>

        <details class="card">
          <summary>Can we travel to the future?</summary>
          <p>
            In a sense, yes: time dilation means a fast traveler can experience less time than people who stayed behind.
            That is “future travel” relative to them — without breaking physics.
          </p>
        </details>

        <details class="card">
          <summary>Can we travel to the past?</summary>
          <p>
            Nothing in everyday tested physics allows practical backward time travel. Some theoretical solutions exist,
            but they come with extreme conditions and unresolved paradoxes.
          </p>
        </details>
      </div>
    </section>

    <!-- FOOT NAV -->
    <nav class="footnav">
      <a class="btn" href="/science/black-holes">← Previous: Black Holes</a>
      <a class="btn btn--ghost" href="/science/quantum-basics">Next: Quantum Basics →</a>
    </nav>
  </article>

  <style>
    .page{ max-width: 980px; margin: 0 auto; padding: 28px 18px 90px; }
    .hero{ padding-top: 6px; }
    .hero-top{ display:flex; align-items:center; justify-content: space-between; gap: 12px; margin-bottom: 14px; }
    .back{ color: rgba(11,11,15,0.60); font-size: 13px; font-weight: 650; text-decoration: none; }
    .back:hover{ color: rgba(11,11,15,0.90); }

    h1{ margin: 0 0 10px; font-size: 44px; font-weight: 850; letter-spacing: -0.035em; line-height: 1.05; }
    .subtitle{ margin: 0 0 18px; font-size: 14px; line-height: 1.6; color: rgba(11,11,15,0.62); max-width: 78ch; }

    .hero-grid{ display:grid; grid-template-columns: 1.1fr 0.9fr; gap: 14px; margin-top: 16px; }

    .section{ margin-top: 28px; }
    .section > h2{ margin: 0 0 12px; font-size: 20px; font-weight: 850; letter-spacing: -0.02em; color: rgba(11,11,15,0.92); }
    .lead{ margin: 0 0 14px; font-size: 15px; line-height: 1.7; color: rgba(11,11,15,0.70); max-width: 84ch; }

    .card{
      background: rgba(255,255,255,0.92);
      border: 1px solid rgba(0,0,0,0.06);
      border-radius: 18px;
      box-shadow: 0 10px 34px rgba(0,0,0,0.06);
      padding: 16px 18px;
    }
    .card--soft{ background: rgba(255,255,255,0.85); }
    .card--ink{
      background: rgba(11,11,15,0.92);
      border-color: rgba(255,255,255,0.08);
      color: rgba(255,255,255,0.92);
      box-shadow: 0 12px 40px rgba(0,0,0,0.12);
    }
    .quote{ margin: 0; font-size: 14px; line-height: 1.7; color: rgba(255,255,255,0.92); }
    .meta{ margin: 12px 0 0; font-size: 12px; color: rgba(255,255,255,0.70); }

    .hsmall{ margin: 0 0 10px; font-size: 13px; font-weight: 900; letter-spacing: .12em; text-transform: uppercase; color: rgba(11,11,15,0.72); }
    .list{ margin: 0; padding-left: 18px; color: rgba(11,11,15,0.72); line-height: 1.65; font-size: 14px; }

    .chip{
      font-size: 11px;
      font-weight: 900;
      letter-spacing: .14em;
      text-transform: uppercase;
      padding: 7px 10px;
      border-radius: 999px;
      border: 1px solid rgba(0,0,0,0.08);
      user-select: none;
      background: rgba(255,255,255,0.70);
      backdrop-filter: blur(10px);
    }
    .chip--physics{
      color: rgba(0,122,255,0.95);
      border-color: rgba(0,122,255,0.20);
      background: rgba(0,122,255,0.12);
    }

    .tldr{ display:grid; gap: 12px; }
    .tldr-item{
      display:flex; gap: 12px; align-items:flex-start;
      padding: 14px 16px; border-radius: 18px;
      background: rgba(255,255,255,0.92);
      border: 1px solid rgba(0,0,0,0.06);
      box-shadow: 0 10px 34px rgba(0,0,0,0.06);
    }
    .tldr-item h3{ margin: 0 0 4px; font-size: 15px; font-weight: 850; letter-spacing: -0.01em; }
    .tldr-item p{ margin: 0; font-size: 14px; line-height: 1.6; color: rgba(11,11,15,0.62); }

    .dot{ width: 10px; height: 10px; border-radius: 999px; margin-top: 5px; flex-shrink:0; }
    .dot--a{ background: rgba(0,122,255,0.95); box-shadow: 0 0 0 4px rgba(0,122,255,0.12); }
    .dot--b{ background: rgba(255,45,85,0.95); box-shadow: 0 0 0 4px rgba(255,45,85,0.12); }
    .dot--c{ background: rgba(52,199,89,0.95); box-shadow: 0 0 0 4px rgba(52,199,89,0.12); }

    .two-col{ display:grid; grid-template-columns: 1fr 1fr; gap: 14px; align-items:start; margin-top: 12px; }
    .stack{ display:flex; flex-direction:column; gap: 10px; }

    .callout{
      padding: 12px 14px;
      border-radius: 16px;
      background: rgba(245,245,247,0.85);
      border: 1px solid rgba(0,0,0,0.06);
    }
    .callout h4{ margin: 0 0 4px; font-size: 13px; font-weight: 850; letter-spacing: -0.01em; color: rgba(11,11,15,0.86); }
    .callout p{ margin: 0; font-size: 14px; line-height: 1.6; color: rgba(11,11,15,0.62); }

    .figure svg{ width: 100%; height: auto; border-radius: 14px; display:block; overflow:hidden; }
    .figcap{ margin: 0 0 10px; font-size: 13px; color: rgba(11,11,15,0.70); }
    .note{ margin: 10px 0 0; font-size: 13px; line-height: 1.6; color: rgba(11,11,15,0.55); }

    .grid3{ display:grid; grid-template-columns: repeat(3, 1fr); gap: 12px; margin-top: 8px; }
    .mini{
      padding: 12px 14px;
      border-radius: 16px;
      background: rgba(245,245,247,0.70);
      border: 1px solid rgba(0,0,0,0.06);
    }
    .mini h4{ margin: 0 0 6px; font-size: 14px; font-weight: 850; letter-spacing: -0.01em; }
    .mini p{ margin: 0; font-size: 13.5px; line-height: 1.6; color: rgba(11,11,15,0.62); }

    .steps{ margin: 10px 0 0; padding-left: 18px; color: rgba(11,11,15,0.70); line-height: 1.7; font-size: 14px; }

    .faq{ display:flex; flex-direction:column; gap: 10px; }
    details summary{ cursor: pointer; font-weight: 850; font-size: 14px; color: rgba(11,11,15,0.86); list-style: none; }
    details summary::-webkit-details-marker{ display:none; }
    details p{ margin: 10px 0 0; font-size: 14px; line-height: 1.7; color: rgba(11,11,15,0.62); }

    .footnav{ margin-top: 28px; display:flex; justify-content: space-between; gap: 12px; }
    .btn{
      display:inline-flex; align-items:center; justify-content:center;
      padding: 10px 12px; border-radius: 999px;
      border: 1px solid rgba(0,0,0,0.10);
      background: rgba(255,255,255,0.90);
      font-size: 13px; font-weight: 750;
      color: rgba(11,11,15,0.84);
      text-decoration:none;
      transition: transform .15s ease, background .15s ease;
      white-space: nowrap;
    }
    .btn:hover{ transform: translateY(-1px); background: rgba(255,255,255,1); }
    .btn--ghost{ background: rgba(245,245,247,0.75); }

    @media (max-width: 900px){
      .hero-grid{ grid-template-columns: 1fr; }
      .two-col{ grid-template-columns: 1fr; }
      .grid3{ grid-template-columns: 1fr; }
    }
    @media (max-width: 480px){
      h1{ font-size: 34px; }
      .footnav{ flex-direction: column; }
      .btn{ width: 100%; }
    }
  </style>
</Layout>
