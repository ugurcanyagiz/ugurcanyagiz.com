<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>UGURCAN — Lifehacker‑style Layout</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#f5f5f7;
      --panel:#ffffff;
      --text:#111;
      --muted:#666;
      --accent:#00a86b;
      --line:#e5e5e7;
      --container:1200px;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial;
      background:var(--bg);
      color:var(--text);
    }
    a{text-decoration:none;color:inherit}

    /* HEADER */
    header{
      background:#fff;
      border-bottom:1px solid var(--line);
    }
    .topbar{
      max-width:var(--container);
      margin:0 auto;
      padding:16px 20px;
      display:flex;
      align-items:center;
      justify-content:space-between;
    }
    .logo{
      font-weight:800;
      letter-spacing:.2em;
      text-transform:uppercase;
      font-size:14px;
    }
    nav{
      display:flex;
      gap:18px;
      font-size:13px;
      color:var(--muted);
    }
    nav a:hover{color:var(--text)}

    /* MAIN GRID */
    main{
      max-width:var(--container);
      margin:24px auto 64px;
      padding:0 20px;
      display:grid;
      grid-template-columns:3fr 1.2fr;
      gap:28px;
    }

    /* FEATURED */
    .featured{
      background:var(--panel);
      border:1px solid var(--line);
    }
    .featured img{
      width:100%;
      height:420px;
      object-fit:cover;
      display:block;
    }
    .featured-content{
      padding:20px;
    }
    .tag{
      color:var(--accent);
      font-size:12px;
      font-weight:600;
      text-transform:uppercase;
    }
    .featured h1{
      margin:10px 0 8px;
      font-size:32px;
      line-height:1.2;
    }
    .meta{
      font-size:12px;
      color:var(--muted);
    }

    /* STORY GRID */
    .stories{
      margin-top:24px;
      display:grid;
      grid-template-columns:1fr 1fr;
      gap:20px;
    }
    .card{
      background:#fff;
      border:1px solid var(--line);
    }
    .card img{
      width:100%;
      height:160px;
      object-fit:cover;
    }
    .card-body{padding:14px}
    .card h3{margin:6px 0 4px;font-size:16px}

    /* SIDEBAR */
    aside{
      display:flex;
      flex-direction:column;
      gap:20px;
    }
    .sidebar-box{
      background:#fff;
      border:1px solid var(--line);
      padding:16px;
    }
    .sidebar-box h4{
      margin:0 0 12px;
      font-size:14px;
      text-transform:uppercase;
      letter-spacing:.08em;
      color:var(--accent);
    }
    .latest-item{
      display:flex;
      gap:10px;
      margin-bottom:12px;
    }
    .latest-item img{
      width:56px;
      height:56px;
      object-fit:cover;
    }
    .latest-item p{
      margin:0;
      font-size:13px;
      line-height:1.4;
    }

    /* MOBILE */
    @media(max-width:900px){
      main{grid-template-columns:1fr}
      .stories{grid-template-columns:1fr}
      nav{display:none}
    }
  </style>
</head>
<body>

<header>
  <div class="topbar">
    <div class="logo">UGURCAN</div>
    <nav>
      <a href="#">Latest</a>
      <a href="#">Science</a>
      <a href="#">Technology</a>
      <a href="#">Art</a>
      <a href="#">History</a>
      <a href="#">JLOG</a>
    </nav>
  </div>
</header>

<main>
  <section>
    <article class="featured">
      <img src="https://images.unsplash.com/photo-1518770660439-4636190af475?w=1600&q=80" alt="">
      <div class="featured-content">
        <span class="tag">Technology</span>
        <h1>We Now Know What Changed Everything</h1>
        <div class="meta">By Ugurcan · Jan 2026</div>
      </div>
    </article>

    <div class="stories">
      <article class="card">
        <img src="https://images.unsplash.com/photo-1460661419201-fd4cecdf8a8b?w=1200&q=80" alt="">
        <div class="card-body">
          <span class="tag">Art</span>
          <h3>Why Taste Matters More Than Tools</h3>
        </div>
      </article>
      <article class="card">
        <img src="https://images.unsplash.com/photo-1461360228754-6e81c478b882?w=1200&q=80" alt="">
        <div class="card-body">
          <span class="tag">History</span>
          <h3>Patterns Civilizations Keep Repeating</h3>
        </div>
      </article>
    </div>
  </section>

  <aside>
    <div class="sidebar-box">
      <h4>Latest</h4>
      <div class="latest-item">
        <img src="https://images.unsplash.com/photo-1520970014086-2208d157c9e2?w=200&q=80" alt="">
        <p>How AI Is Quietly Changing Writing</p>
      </div>
      <div class="latest-item">
        <img src="https://images.unsplash.com/photo-1455390582262-044cdead277a?w=200&q=80" alt="">
        <p>Notes From a Long Walk About Time</p>
      </div>
    </div>
  </aside>
</main>

</body>
</html>
