---
import Layout from "../layouts/Layout.astro";

const audioSrc = "/audio/pale-blue-dot.mp3";

// Audio üstündeki küçük başlıkları (kart içinde) istersen tutuyoruz, bunlar sayfa başlığı değil:
const audioTitle = "The Pale Blue Dot";
const audioSubtitle = "Carl Sagan · Audio recording";

// Quote (audio altında)
const quote = "I don't want to believe. I want to know.";

// Blog cards
const posts = [
  {
    href: "/jlog/story-of-my-life",
    category: "LIFE",
    title: "the story of my life",
    excerpt: "A personal thread: curiosity, migration, building, learning — and becoming.",
    // Seçenek A:
    img: "/images/story-of-my-life.jpg",
  },
  {
    href: "/jlog/what-is-real",
    category: "LIFE",
    title: "do we really know what is real?",
    excerpt: "Perception, dreams, memory, and the strange edges of certainty.",
    // Seçenek A (önerilen):
    img: "/images/reality-dali.jpg",
    // Seçenek B (şimdilik): aşağıdaki satırı açıp üsttekini kapatabilirsin
    // img: "https://images.unsplash.com/photo-1541963463532-d68292c34b19?w=1600&q=80",
  },
  {
    href: "/jlog/idiots-of-tomorrow",
    category: "THOUGHT",
    title: "we are the idiots of tomorrow.",
    excerpt: "Humility across time. What feels obvious today may look naive tomorrow.",
    img: "/images/idiots.jpg",
  },
  {
    href: "/jlog/we-must-believe",
    category: "BELIEF",
    title: "we must believe (?)",
    excerpt: "From ancient gods to modern systems — why humans feel compelled to believe in something.",
    img: "/images/din.jpg",
  },
];

const filters = ["ALL", "LIFE", "THOUGHT", "BELIEF" ];
---

<Layout title="JLOG! — Ugurcan">
  <main class="jlog">
    <!-- HEADER (sadece JLOG!) -->
    <header class="head" aria-label="JLOG header">
      <p class="kicker">JLOG!</p>
    </header>

    <!-- AUDIO CARD (aynı tasarım kalsın) -->
    <section class="card" aria-label="Audio recording">
      <div class="card-top">
        <div class="wave" aria-hidden="true">
          <svg viewBox="0 0 120 24" width="120" height="24">
            <path
              d="M2 12c6 0 6-8 12-8s6 16 12 16 6-16 12-16 6 16 12 16 6-16 12-16 6 16 12 16 6-16 12-16 6 16 12 16 6-16 12-16 6 8 12 8"
              fill="none"
              stroke="currentColor"
              stroke-width="2.2"
              stroke-linecap="round"
              opacity=".55"
            />
          </svg>
        </div>

        <div class="card-title">
          <div class="small">{audioSubtitle}</div>
          <div class="big">{audioTitle}</div>
        </div>

        <a class="download" href={audioSrc} download aria-label="Download audio">Download</a>
      </div>

      <div class="player" aria-label="Audio controls">
        <button class="play" type="button" aria-label="Play/Pause" id="playBtn">▶</button>

        <div class="time" id="tNow">0:00</div>

        <input class="seek" id="seek" type="range" min="0" max="1000" value="0" step="1" aria-label="Seek" />

        <div class="time" id="tDur">0:00</div>

        <button class="mute" type="button" aria-label="Mute/Unmute" id="muteBtn">🔈</button>
      </div>

      <div class="autoplay-hint" id="hint" aria-live="polite"></div>

      <audio id="audio" preload="metadata" playsinline>
        <source src={audioSrc} type="audio/mpeg" />
        Your browser does not support the audio element.
      </audio>
    </section>

    <!-- QUOTE (audio altı) -->
    <section class="quote" aria-label="Quote">
      <p class="quote-text">“{quote}”</p>
    </section>

    <!-- FILTER -->
    <nav class="filterbar" aria-label="JLOG filters">
      <div class="rail" role="toolbar" aria-label="Filter options">
        {filters.map((f) => (
          <button
            class={`pill ${f === "ALL" ? "active" : ""}`}
            type="button"
            data-filter={f}
            aria-pressed={f === "ALL" ? "true" : "false"}
          >
            {f}
          </button>
        ))}
      </div>
    </nav>

    <!-- POSTS -->
    <section class="posts" aria-label="JLOG posts">
      {posts.map((p) => (
        <a class="post-card" href={p.href} data-category={p.category} style={`--bg:url('${p.img}')`}>
          <span class={`badge badge--${p.category}`}>{p.category}</span>

          <div class="overlay">
            <h2>{p.title}</h2>
            <p>{p.excerpt}</p>
          </div>
        </a>
      ))}
    </section>
  </main>

  <style>
    .jlog{
      max-width: 1100px;
      margin: 0 auto;
      padding: 18px 18px 90px;
      display: grid;
      gap: 16px;
    }

    .head{
      text-align: center;
      padding-top: 18px;
    }

    .kicker{
      margin: 0;
      font-size: 12px;
      font-weight: 900;
      letter-spacing: .26em;
      text-transform: uppercase;
      color: rgba(11,11,15,0.62);
    }

    /* AUDIO CARD */
    .card{
      width: min(980px, 100%);
      margin: 0 auto;
      background: rgba(255,255,255,0.92);
      border: 1px solid rgba(0,0,0,0.06);
      border-radius: 22px;
      box-shadow: 0 14px 48px rgba(0,0,0,0.07);
      padding: 16px 16px 14px;
      overflow: hidden;
    }

    .card-top{
      display:flex;
      align-items:center;
      gap: 14px;
      padding-bottom: 12px;
      border-bottom: 1px solid rgba(0,0,0,0.06);
      margin-bottom: 12px;
    }

    .wave{
      width: 44px;
      height: 44px;
      border-radius: 12px;
      display:flex;
      align-items:center;
      justify-content:center;
      background: rgba(245,245,247,0.9);
      border: 1px solid rgba(0,0,0,0.06);
      color: rgba(11,11,15,0.55);
      flex-shrink: 0;
    }

    .card-title{ min-width:0; flex:1; }

    .small{
      font-size: 12px;
      font-weight: 650;
      color: rgba(11,11,15,0.55);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .big{
      margin-top: 2px;
      font-size: 16px;
      font-weight: 850;
      letter-spacing: -0.02em;
      color: rgba(11,11,15,0.90);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .download{
      flex-shrink:0;
      padding: 9px 12px;
      border-radius: 10px;
      border: 1px solid rgba(0,0,0,0.10);
      background: rgba(255,255,255,0.90);
      color: rgba(11,11,15,0.78);
      font-size: 13px;
      font-weight: 700;
      text-decoration: none;
      transition: transform .15s ease, background .15s ease;
    }
    .download:hover{
      transform: translateY(-1px);
      background: rgba(255,255,255,1);
      color: rgba(11,11,15,0.92);
    }

    .player{
      display:flex;
      align-items:center;
      gap: 12px;
      padding: 2px 2px 4px;
    }

    .play, .mute{
      width: 38px;
      height: 38px;
      border-radius: 999px;
      border: 1px solid rgba(0,0,0,0.10);
      background: rgba(255,255,255,0.92);
      color: rgba(11,11,15,0.86);
      cursor: pointer;
      display:flex;
      align-items:center;
      justify-content:center;
      transition: transform .15s ease, background .15s ease;
      user-select:none;
    }
    .play:hover, .mute:hover{
      transform: translateY(-1px);
      background: rgba(255,255,255,1);
    }

    .time{
      font-variant-numeric: tabular-nums;
      font-size: 12px;
      font-weight: 750;
      color: rgba(11,11,15,0.58);
      min-width: 44px;
      text-align:center;
      user-select:none;
    }

    .seek{
      flex: 1;
      -webkit-appearance: none;
      appearance: none;
      height: 6px;
      border-radius: 999px;
      background: rgba(0,0,0,0.12);
      outline: none;
    }
    .seek::-webkit-slider-thumb{
      -webkit-appearance:none;
      appearance:none;
      width: 14px;
      height: 14px;
      border-radius: 999px;
      background: rgba(11,11,15,0.85);
      border: 2px solid rgba(255,255,255,0.90);
      box-shadow: 0 6px 18px rgba(0,0,0,0.15);
      cursor: pointer;
    }
    .seek::-moz-range-thumb{
      width: 14px;
      height: 14px;
      border-radius: 999px;
      background: rgba(11,11,15,0.85);
      border: 2px solid rgba(255,255,255,0.90);
      box-shadow: 0 6px 18px rgba(0,0,0,0.15);
      cursor: pointer;
    }

    .autoplay-hint{
      margin: 6px 2px 0;
      font-size: 12px;
      line-height: 1.45;
      color: rgba(11,11,15,0.52);
      min-height: 18px;
    }

    /* QUOTE */
    .quote{
      width: min(980px, 100%);
      margin: 0 auto;
      text-align: center;
      padding: 10px 6px 2px;
    }
    .quote-text{
      margin: 0;
      font-size: 13px;
      font-weight: 650;
      letter-spacing: -0.01em;
      color: rgba(11,11,15,0.62);
    }

    /* FILTER BAR (Apple-like, yatay kaydırmalı) */
    .filterbar{
      width: min(980px, 100%);
      margin: 6px auto 0;
      padding: 6px 0 0;
    }

    .rail{
      display:flex;
      gap:10px;
      overflow-x:auto;
      overflow-y:hidden;
      -webkit-overflow-scrolling:touch;
      padding: 2px 2px 8px;
      scrollbar-width:none;
    }
    .rail::-webkit-scrollbar{ display:none; }

    .pill{
      flex:0 0 auto;
      appearance:none;
      border: 1px solid rgba(0,0,0,.10);
      background: rgba(255,255,255,.88);
      color: rgba(11,11,15,.72);
      border-radius: 999px;
      padding: 9px 12px;
      font-size: 12.5px;
      font-weight: 700;
      letter-spacing: .08em;
      text-transform: uppercase;
      cursor:pointer;
      transition: transform .15s ease, background .15s ease, color .15s ease, border-color .15s ease;
      user-select:none;
      white-space: nowrap;
    }
    .pill:hover{
      transform: translateY(-1px);
      background: rgba(255,255,255,1);
      color: rgba(11,11,15,.90);
      border-color: rgba(0,0,0,.14);
    }
    .pill.active{
      background: rgba(11,11,15,.92);
      color: rgba(255,255,255,.94);
      border-color: rgba(11,11,15,.18);
    }

    /* POSTS */
    .posts{
      width: min(980px, 100%);
      margin: 6px auto 0;
      display: grid;
      gap: 14px;
    }

    .post-card{
      position: relative;
      border-radius: 22px;
      overflow: hidden;
      min-height: 180px;
      border: 1px solid rgba(0,0,0,0.06);
      box-shadow: 0 18px 60px rgba(0,0,0,0.10);
      background: #111;
      text-decoration: none;
      display: block;

      background-image: var(--bg);
      background-size: cover;
      background-position: center;
      transform: translateZ(0);
    }

    .post-card::before{
      content:"";
      position:absolute;
      inset:0;
      background:
        radial-gradient(1200px 360px at 22% 80%, rgba(0,0,0,0.55), transparent 55%),
        linear-gradient(180deg, rgba(0,0,0,0.10), rgba(0,0,0,0.58));
      pointer-events:none;
    }

    .overlay{
      position:absolute;
      inset:auto 0 0 0;
      padding: 18px 18px 16px;
      z-index: 2;
    }

    .overlay h2{
      margin: 0 0 6px;
      font-size: 22px;
      font-weight: 900;
      letter-spacing: -0.03em;
      color: rgba(255,255,255,0.96);
      text-shadow: 0 10px 30px rgba(0,0,0,0.45);
    }

    .overlay p{
      margin: 0;
      font-size: 13.5px;
      line-height: 1.5;
      color: rgba(255,255,255,0.80);
      max-width: 70ch;
    }

    .badge{
      position:absolute;
      top: 14px;
      right: 14px;
      z-index: 3;
      padding: 7px 10px;
      border-radius: 999px;
      font-size: 11px;
      font-weight: 900;
      letter-spacing: .14em;
      text-transform: uppercase;
      background: rgba(255,255,255,0.82);
      color: rgba(11,11,15,0.80);
      border: 1px solid rgba(0,0,0,0.08);
      backdrop-filter: blur(10px);
      user-select:none;
    }

    /* filter toggles */
    .post-card.is-hidden{ display: none; }

    @media (max-width: 520px){
      .time{ min-width: 38px; }
      .download{ padding: 9px 10px; }
      .overlay h2{ font-size: 18px; }
      .post-card{ min-height: 170px; }
    }
  </style>

  <script is:inline>
    // AUDIO LOGIC
    const audio = document.getElementById("audio");
    const playBtn = document.getElementById("playBtn");
    const muteBtn = document.getElementById("muteBtn");
    const seek = document.getElementById("seek");
    const tNow = document.getElementById("tNow");
    const tDur = document.getElementById("tDur");
    const hint = document.getElementById("hint");

    function fmt(s){
      if (!isFinite(s)) return "0:00";
      const m = Math.floor(s / 60);
      const r = Math.floor(s % 60);
      return `${m}:${String(r).padStart(2,"0")}`;
    }

    function update(){
      const dur = audio.duration || 0;
      const cur = audio.currentTime || 0;
      tNow.textContent = fmt(cur);
      tDur.textContent = fmt(dur);
      if (dur > 0){
        const v = Math.round((cur / dur) * 1000);
        seek.value = String(v);
      }
    }

    function setPlaying(on){
      playBtn.textContent = on ? "❚❚" : "▶";
    }

    async function tryAutoplay(){
      try{
        await audio.play();
        setPlaying(true);
        hint.textContent = "";
      }catch(e){
        setPlaying(false);
        hint.textContent = "Autoplay may be blocked by your browser. Press Play to start.";
      }
    }

    audio.addEventListener("loadedmetadata", update);
    audio.addEventListener("timeupdate", update);
    audio.addEventListener("play", () => setPlaying(true));
    audio.addEventListener("pause", () => setPlaying(false));
    audio.addEventListener("ended", () => setPlaying(false));

    playBtn.addEventListener("click", async () => {
      if (audio.paused){
        try { await audio.play(); } catch(e) {}
      } else {
        audio.pause();
      }
    });

    muteBtn.addEventListener("click", () => {
      audio.muted = !audio.muted;
      muteBtn.textContent = audio.muted ? "🔇" : "🔈";
    });

    seek.addEventListener("input", () => {
      const dur = audio.duration || 0;
      if (dur <= 0) return;
      const v = Number(seek.value) / 1000;
      audio.currentTime = v * dur;
    });

    if (document.readyState === "loading"){
      document.addEventListener("DOMContentLoaded", () => {
        update();
        tryAutoplay();
      }, { once: true });
    } else {
      update();
      tryAutoplay();
    }

    // FILTER LOGIC (posts)
    const pills = Array.from(document.querySelectorAll(".pill"));
    const cards = Array.from(document.querySelectorAll(".post-card"));

    function apply(filter){
      pills.forEach(p => {
        const active = p.dataset.filter === filter;
        p.classList.toggle("active", active);
        p.setAttribute("aria-pressed", active ? "true" : "false");
      });

      cards.forEach(card => {
        const cat = card.dataset.category;
        const show = (filter === "ALL") || (cat === filter);
        card.classList.toggle("is-hidden", !show);
      });
    }

    pills.forEach(p => p.addEventListener("click", () => apply(p.dataset.filter)));
    apply("ALL");
  </script>
</Layout>
