---
import Layout from "../../layouts/Layout.astro";

/**
 * JLOG entry: we must believe (?)
 * Images:
 * - Put your main image here:
 *   public/images/din.jpg
 *
 * Optional (if you want to add later):
 *   public/images/jlog/belief/ancient.jpg
 *   public/images/jlog/belief/temple.jpg
 *   public/images/jlog/belief/science.jpg
 *   public/images/jlog/belief/tech.jpg
 */

const hero = {
  kicker: "JLOG!",
  title: "we must believe (?)",
  meta: "Belief · Culture · Evolution · Technology",
  cover: "/images/din.jpg",
  coverAlt: "A symbolic image representing belief across history",
};
---

<Layout title="we must believe (?) — JLOG!">
  <main class="page">
    <!-- HERO -->
    <header class="hero" aria-label="JLOG entry header">
      <p class="kicker">{hero.kicker}</p>

      <h1 class="h1">{hero.title}</h1>
      <p class="meta">{hero.meta}</p>

      <figure class="cover" aria-label="Cover image">
        <img src={hero.cover} alt={hero.coverAlt} loading="eager" />
        <div class="coverFade" aria-hidden="true"></div>
      </figure>
    </header>

    <!-- INTRO -->
    <section class="paper" aria-label="Essay">
      <p class="lede">
        Humans don’t only want answers — we want <em>meaning</em>.
        When nature is loud and uncertain, the mind builds a story that makes the world livable.
        That story becomes belief. Belief becomes structure. Structure becomes culture.
      </p>

      <div class="body">
        <p>
          This page is a short map of a long journey: from ancient spirits to organized religions,
          from philosophy to science, from modern “gods” like ideology and money to a future that
          may be less about worship — and more about understanding.
        </p>

        <div class="callout" role="note" aria-label="Key idea">
          <p class="calloutTitle">A simple hypothesis</p>
          <p class="calloutText">
            Belief systems tend to expand when uncertainty is high and contract when reliable explanations become common.
            But belief doesn’t disappear — it <em>migrates</em>.
          </p>
        </div>
      </div>
    </section>

    <!-- ANIMATION: Belief → Knowledge Map -->
    <section class="viz" aria-label="Belief-to-knowledge timeline">
      <div class="vizHead">
        <h2 class="h2">A minimal timeline of belief</h2>
        <p class="sub">
          Not “true vs false” — more like a changing interface between uncertainty, story, and explanation.
        </p>
      </div>

      <div class="timeline" role="img" aria-label="Animated timeline from myth to knowledge">
        <div class="track" aria-hidden="true"></div>

        <!-- Stops -->
        <div class="stop s1">
          <div class="dot"></div>
          <div class="label">
            <div class="labelTop">Myth & spirits</div>
            <div class="labelBot">fear → story</div>
          </div>
        </div>

        <div class="stop s2">
          <div class="dot"></div>
          <div class="label">
            <div class="labelTop">Gods & temples</div>
            <div class="labelBot">ritual → order</div>
          </div>
        </div>

        <div class="stop s3">
          <div class="dot"></div>
          <div class="label">
            <div class="labelTop">Monotheism</div>
            <div class="labelBot">unity → law</div>
          </div>
        </div>

        <div class="stop s4">
          <div class="dot"></div>
          <div class="label">
            <div class="labelTop">Philosophy</div>
            <div class="labelBot">reason → argument</div>
          </div>
        </div>

        <div class="stop s5">
          <div class="dot"></div>
          <div class="label">
            <div class="labelTop">Science</div>
            <div class="labelBot">test → predict</div>
          </div>
        </div>

        <div class="stop s6">
          <div class="dot"></div>
          <div class="label">
            <div class="labelTop">Technology</div>
            <div class="labelBot">tools → power</div>
          </div>
        </div>

        <div class="stop s7">
          <div class="dot"></div>
          <div class="label">
            <div class="labelTop">Future culture</div>
            <div class="labelBot">humility → care</div>
          </div>
        </div>

        <!-- Moving cursor (animated) -->
        <div class="cursor" aria-hidden="true">
          <div class="cursorRing"></div>
        </div>

        <div class="legend" aria-hidden="true">
          <span class="pill p-belief">belief</span>
          <span class="pill p-knowledge">knowledge</span>
          <span class="pill p-meaning">meaning</span>
        </div>
      </div>
    </section>

    <!-- SECTIONS -->
    <section class="paper" aria-label="Main sections">
      <h2 class="h2">Why we “must” believe</h2>
      <p class="p">
        In the earliest ages, nature didn’t offer explanations — it offered consequences.
        Lightning burns. Winter kills. Disease arrives without warning. The mind hates a vacuum.
        So the first “gods” were often names for patterns we couldn’t control.
      </p>

      <div class="grid2" aria-label="Key patterns">
        <article class="tile">
          <h3 class="h3">Uncertainty</h3>
          <p class="p">
            When you can’t predict, you negotiate — with rituals, prayers, symbols, taboos.
            It’s a psychological insurance policy.
          </p>
        </article>

        <article class="tile">
          <h3 class="h3">Coordination</h3>
          <p class="p">
            Shared belief is a social technology. It lets millions cooperate, trust, punish, forgive,
            and build identities bigger than the family.
          </p>
        </article>

        <article class="tile">
          <h3 class="h3">Meaning</h3>
          <p class="p">
            Even when we can predict the world, we still ask: “Why should I live well?”
            Meaning doesn’t fall out of physics.
          </p>
        </article>

        <article class="tile">
          <h3 class="h3">Power</h3>
          <p class="p">
            Belief systems can be used to protect people — or to control them.
            The same structure that holds a society can also cage it.
          </p>
        </article>
      </div>

      <hr class="sep" />

      <h2 class="h2">Modern gods</h2>
      <p class="p">
        As science explains more of nature, many traditional claims lose public authority.
        But the need for certainty doesn’t vanish. It often attaches to new objects:
        ideologies, brands, money, nationalism, “progress,” even technology itself.
      </p>

      <div class="chips" aria-label="Modern gods list">
        <span class="chip">Nation</span>
        <span class="chip">Ideology</span>
        <span class="chip">Money</span>
        <span class="chip">Status</span>
        <span class="chip">Influence</span>
        <span class="chip">Techno-optimism</span>
        <span class="chip">Conspiracies</span>
        <span class="chip">Identity tribes</span>
      </div>

      <div class="quote" aria-label="Quote">
        <p class="q">
          “The more we explain, the less we worship — but the more we risk worshipping our explanations.”
        </p>
        <p class="a">— a note to myself</p>
      </div>

      <hr class="sep" />

      <h2 class="h2">A culture where new gods don’t form</h2>
      <p class="p">
        Maybe the next step is not “no belief,” but <em>better humility</em>:
        an adult relationship with uncertainty. Not cynicism — clarity.
        A culture that can say “I don’t know” without panic.
      </p>

      <div class="ending" aria-label="Closing reflection">
        <p class="p">
          If the future is wise, it won’t replace old gods with new idols.
          It will replace certainty with curiosity, and fear with responsibility —
          toward each other, and toward the fragile world that holds all of us.
        </p>
      </div>
    </section>

    <!-- FOOTNOTE -->
    <section class="foot" aria-label="Footer note">
      <p class="footText">
        Note: This is a personal synthesis, not a claim of final truth. The point is to keep the question alive —
        without turning it into a weapon.
      </p>
    </section>
  </main>

  <style>
    /* Page frame */
    .page{
      max-width: 1100px;
      margin: 0 auto;
      padding: 18px 18px 90px;
      display: grid;
      gap: 18px;
    }

    /* HERO */
    .hero{
      text-align: center;
      padding-top: 10px;
      display: grid;
      gap: 10px;
      justify-items: center;
    }

    .kicker{
      margin: 0;
      font-size: 12px;
      font-weight: 900;
      letter-spacing: .26em;
      text-transform: uppercase;
      color: rgba(11,11,15,0.62);
    }

    .h1{
      margin: 0;
      font-size: 46px;
      font-weight: 850;
      letter-spacing: -0.04em;
      line-height: 1.05;
      color: rgba(11,11,15,0.92);
      text-wrap: balance;
    }

    .meta{
      margin: 0;
      font-size: 14px;
      line-height: 1.6;
      color: rgba(11,11,15,0.60);
    }

    .cover{
      width: min(980px, 100%);
      margin: 10px auto 0;
      border-radius: 24px;
      overflow: hidden;
      position: relative;
      background: rgba(255,255,255,0.7);
      border: 1px solid rgba(0,0,0,0.06);
      box-shadow: 0 18px 60px rgba(0,0,0,.10);
      aspect-ratio: 2.2 / 1;
    }
    .cover img{
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
      transform: scale(1.02);
    }
    .coverFade{
      position:absolute;
      inset:0;
      background: radial-gradient(120% 90% at 50% 0%, rgba(0,0,0,0.15), rgba(0,0,0,0.00) 60%),
                  linear-gradient(to top, rgba(0,0,0,0.34), rgba(0,0,0,0.00) 55%);
      pointer-events:none;
    }

    /* Paper */
    .paper{
      width: min(980px, 100%);
      margin: 0 auto;
      background: rgba(255,255,255,0.92);
      border: 1px solid rgba(0,0,0,0.06);
      border-radius: 24px;
      box-shadow: 0 14px 48px rgba(0,0,0,0.07);
      padding: 22px 22px 20px;
    }

    .lede{
      margin: 0 0 14px;
      font-size: 16px;
      line-height: 1.75;
      color: rgba(11,11,15,0.80);
      letter-spacing: -0.01em;
    }
    .body{ display: grid; gap: 14px; }

    .h2{
      margin: 0 0 8px;
      font-size: 18px;
      font-weight: 850;
      letter-spacing: -0.02em;
      color: rgba(11,11,15,0.92);
    }
    .h3{
      margin: 0 0 6px;
      font-size: 14px;
      font-weight: 850;
      letter-spacing: -0.01em;
      color: rgba(11,11,15,0.92);
    }
    .p{
      margin: 0;
      font-size: 14px;
      line-height: 1.75;
      color: rgba(11,11,15,0.68);
    }

    .callout{
      border-radius: 18px;
      padding: 14px 14px;
      background: rgba(245,245,247,0.75);
      border: 1px solid rgba(0,0,0,0.06);
    }
    .calloutTitle{
      margin: 0 0 6px;
      font-size: 12px;
      font-weight: 900;
      letter-spacing: .14em;
      text-transform: uppercase;
      color: rgba(11,11,15,0.62);
    }
    .calloutText{
      margin: 0;
      font-size: 14px;
      line-height: 1.75;
      color: rgba(11,11,15,0.70);
    }

    .sep{
      border: 0;
      height: 1px;
      background: rgba(0,0,0,0.06);
      margin: 16px 0;
    }

    /* Timeline / animation */
    .viz{
      width: min(980px, 100%);
      margin: 0 auto;
      display: grid;
      gap: 10px;
    }

    .vizHead{
      padding: 4px 2px 0;
      text-align: left;
    }
    .sub{
      margin: 0;
      font-size: 13px;
      line-height: 1.6;
      color: rgba(11,11,15,0.58);
      max-width: 70ch;
    }

    .timeline{
      position: relative;
      border-radius: 24px;
      background: rgba(255,255,255,0.92);
      border: 1px solid rgba(0,0,0,0.06);
      box-shadow: 0 14px 48px rgba(0,0,0,0.07);
      padding: 18px 16px 16px;
      overflow: hidden;
      min-height: 190px;
    }

    .track{
      position: absolute;
      left: 18px;
      right: 18px;
      top: 78px;
      height: 8px;
      border-radius: 999px;
      background: linear-gradient(90deg,
        rgba(11,11,15,0.14),
        rgba(11,11,15,0.10) 45%,
        rgba(11,11,15,0.08)
      );
    }

    .stop{
      position: absolute;
      top: 52px;
      transform: translateX(-50%);
      width: 1px;
    }

    .stop .dot{
      width: 10px;
      height: 10px;
      border-radius: 999px;
      background: rgba(11,11,15,0.70);
      box-shadow: 0 0 0 4px rgba(11,11,15,0.08);
      margin: 0 auto 10px;
    }

    .label{
      width: 130px;
      text-align: center;
      transform: translateX(-50%);
      margin-left: 0px;
    }

    .labelTop{
      font-size: 12px;
      font-weight: 850;
      letter-spacing: -0.01em;
      color: rgba(11,11,15,0.86);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .labelBot{
      margin-top: 2px;
      font-size: 12px;
      font-weight: 650;
      color: rgba(11,11,15,0.55);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    /* stop positions */
    .s1{ left: 7%; }
    .s2{ left: 20%; }
    .s3{ left: 35%; }
    .s4{ left: 50%; }
    .s5{ left: 65%; }
    .s6{ left: 80%; }
    .s7{ left: 93%; }

    .cursor{
      position: absolute;
      top: 74px;
      left: 7%;
      width: 20px;
      height: 20px;
      transform: translate(-50%, -50%);
      pointer-events: none;
      animation: glide 10.5s ease-in-out infinite;
    }
    .cursorRing{
      width: 18px;
      height: 18px;
      border-radius: 999px;
      background: rgba(255,255,255,0.85);
      border: 1px solid rgba(0,0,0,0.18);
      box-shadow: 0 10px 30px rgba(0,0,0,0.18);
    }

    @keyframes glide{
      0%   { left: 7%;  }
      22%  { left: 20%; }
      40%  { left: 35%; }
      58%  { left: 50%; }
      76%  { left: 65%; }
      90%  { left: 80%; }
      100% { left: 93%; }
    }

    .legend{
      position: absolute;
      bottom: 14px;
      left: 14px;
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }
    .pill{
      border-radius: 999px;
      padding: 7px 10px;
      font-size: 11px;
      font-weight: 850;
      letter-spacing: .10em;
      text-transform: uppercase;
      border: 1px solid rgba(0,0,0,0.08);
      background: rgba(245,245,247,0.7);
      color: rgba(11,11,15,0.68);
    }
    .p-belief{ background: rgba(175,82,222,0.12); color: rgba(120,50,170,0.92); border-color: rgba(175,82,222,0.18); }
    .p-knowledge{ background: rgba(0,122,255,0.12); color: rgba(0,122,255,0.92); border-color: rgba(0,122,255,0.18); }
    .p-meaning{ background: rgba(255,149,0,0.12); color: rgba(255,149,0,0.92); border-color: rgba(255,149,0,0.18); }

    /* Tiles */
    .grid2{
      margin-top: 14px;
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 12px;
    }
    .tile{
      border-radius: 18px;
      padding: 14px 14px;
      background: rgba(245,245,247,0.75);
      border: 1px solid rgba(0,0,0,0.06);
    }

    .chips{
      margin-top: 12px;
      display:flex;
      flex-wrap:wrap;
      gap: 8px;
    }
    .chip{
      border-radius: 999px;
      padding: 8px 10px;
      font-size: 12px;
      font-weight: 750;
      letter-spacing: -0.01em;
      background: rgba(245,245,247,0.75);
      border: 1px solid rgba(0,0,0,0.06);
      color: rgba(11,11,15,0.70);
      user-select:none;
    }

    .quote{
      margin-top: 14px;
      border-radius: 18px;
      padding: 14px 14px;
      background: rgba(11,11,15,0.04);
      border: 1px solid rgba(0,0,0,0.06);
    }
    .q{
      margin: 0;
      font-size: 14px;
      line-height: 1.75;
      color: rgba(11,11,15,0.76);
    }
    .a{
      margin: 8px 0 0;
      font-size: 12px;
      font-weight: 800;
      color: rgba(11,11,15,0.55);
      letter-spacing: -0.01em;
    }

    .ending{
      margin-top: 12px;
      border-radius: 18px;
      padding: 14px 14px;
      background: rgba(245,245,247,0.75);
      border: 1px solid rgba(0,0,0,0.06);
    }

    .foot{
      width: min(980px, 100%);
      margin: 0 auto;
      padding: 0 4px;
    }
    .footText{
      margin: 0;
      font-size: 12px;
      line-height: 1.65;
      color: rgba(11,11,15,0.52);
    }

    /* Reduced motion */
    @media (prefers-reduced-motion: reduce){
      .cursor{ animation: none; left: 50%; }
    }

    /* Mobile */
    @media (max-width: 720px){
      .h1{ font-size: 36px; }
      .cover{ border-radius: 20px; aspect-ratio: 1.8 / 1; }
      .paper{ padding: 18px 16px 16px; border-radius: 20px; }
      .timeline{ border-radius: 20px; min-height: 240px; }
      .grid2{ grid-template-columns: 1fr; }
      .label{ width: 120px; }
      .track{ top: 98px; }
      .stop{ top: 72px; }
      .cursor{ top: 94px; }
    }

    @media (max-width: 420px){
      .page{ padding: 16px 14px 80px; }
      .h1{ font-size: 32px; }
      .labelTop{ font-size: 11.5px; }
      .labelBot{ font-size: 11.5px; }
    }
  </style>

  <script is:inline>
    // Optional enhancement: pause the cursor when the user hovers the timeline.
    const tl = document.querySelector(".timeline");
    const cursor = document.querySelector(".cursor");
    if (tl && cursor) {
      tl.addEventListener("mouseenter", () => {
        cursor.style.animationPlayState = "paused";
      });
      tl.addEventListener("mouseleave", () => {
        cursor.style.animationPlayState = "running";
      });
    }
  </script>
</Layout>
